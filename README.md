# TinderBOT-AI-neural-network
Try to guess if a girl is pretty or "not that pretty" according to 500 pictures that I categorized (MY TASTES !!)

run pip install -r requirements.txt to install dependencies
